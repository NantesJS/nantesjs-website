import path from 'path'

export const MEETUPS_DIRECTORY = path.join(process.cwd(), 'src/lib/data/meetups')
export const MAIN_DIRECTORY = path.join(process.cwd(), 'src/lib/data')
export const POSTERS_DIRECTORY = path.join(process.cwd(), 'public/posters')
export const TEMPLATE_DIRECTORY = path.join(process.cwd(), 'src/templates')
